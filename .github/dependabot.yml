version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current (bumps the pins + version
  # comments in .github/workflows/). Grouped into a single weekly PR.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    groups:
      actions:
        patterns: ["*"]
